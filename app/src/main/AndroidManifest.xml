<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.OptiSalud"
        tools:targetApi="31">
        <activity
            android:name=".ActivityDudas"
            android:exported="false" />
        <activity
            android:name=".domicilioActivity"
            android:exported="false" />
        <activity
            android:name=".buscarMedActivity"
            android:exported="false" />
        <activity
            android:name=".verMedActivity"
            android:exported="false" />
        <activity
            android:name=".MedicamentosActivity"
            android:exported="true"
            android:label="@string/title_activity_medicamentos"
            android:theme="@style/Theme.OptiSalud" />
        <activity
            android:name=".activity_user"
            android:exported="false" />
        <activity
            android:name=".activity_conf"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".iniciar_activity" />
        <activity android:name=".menu_activity" />
        <activity android:name=".registrar_activity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>